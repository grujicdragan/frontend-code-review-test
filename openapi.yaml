openapi: 3.0.0
info:
  title: Messages API
  version: 1.0.0
  # Add server address API base URL
paths:
  /messages:
    get:
      summary: Get Messages
      description: Retrieves a list of messages.
      responses:
      # Add responses for possible errors such as errors due to server errors or invalid requests
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: "object"
                properties:
                  messages:
                    type: "array"
                    items:
                      type: "object"
                      properties:
                        text:
                          type: "string"
                        status:
                          type: "string"
                          enum:
                            - "sent"
              example:
                messages:
                  - text: "Hello, World!"
                    status: "sent"
                  - text: "How are you today?"
                    status: "sent"
                  - text: "Thanks, I'm fine. How about you?"
                    status: "sent"
  /messages/send:
    get: #  Method should be POST - cannot send a body in request for http GET methods
      summary: Send a Message
      requestBody:
        content:
          application/json:
            schema:
              type: "object"
              properties:
                text:
                  type: "string"
      responses:
      # Add responses for possible errors such as errors due to server errors or invalid requests
        '204': # Should be status code 200 for succesfully sent 
          description: Successfully sent
  # Add security section